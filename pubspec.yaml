name: shiftshield
description: AI-powered income protection for delivery riders — Guidewire DEVTrails 2026

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  # Add these in Phase 2:
  # http: ^1.1.0              — weather/AQI API calls
  # shared_preferences: ^2.2.0 — local persistence
  # firebase_messaging: ^14.7.0 — push notifications
  # razorpay_flutter: ^1.3.5   — payment sandbox

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Add Nunito font via Google Fonts package or download and add assets:
  # fonts:
  #   - family: Nunito
  #     fonts:
  #       - asset: assets/fonts/Nunito-Regular.ttf
  #       - asset: assets/fonts/Nunito-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Nunito-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/Nunito-ExtraBold.ttf
  #         weight: 800